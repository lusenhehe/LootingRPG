/**
 * @deprecated 迁移过渡层：请优先从 `src/domains/map/model/chapters.ts` 引用。
 */
export type {
  NodeWave,
  MapNodeDef,
  MapChapterDef,
} from '../../domains/map/model/chapters';

/**
 * @deprecated 迁移过渡层：请优先从 `src/domains/map/model/chapters.ts` 引用。
 */
export { MAP_CHAPTERS } from '../../domains/map/model/chapters';
