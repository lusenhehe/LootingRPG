/**
 * @deprecated 迁移过渡层：请优先从 `src/domains/inventory/services/equipmentTotals.ts` 引用。
 */
export type { EquipmentTotals } from '../domains/inventory/services/equipmentTotals';

/**
 * @deprecated 迁移过渡层：请优先从 `src/domains/inventory/services/equipmentTotals.ts` 引用。
 */
export { getEquipmentTotals } from '../domains/inventory/services/equipmentTotals';
