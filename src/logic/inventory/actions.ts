/**
 * @deprecated 迁移过渡层：请优先从 `src/domains/inventory/services/actions.ts` 引用。
 */
export type {
  InventoryAction,
  InventoryActionResult,
} from '../../domains/inventory/services/actions';

/**
 * @deprecated 迁移过渡层：请优先从 `src/domains/inventory/services/actions.ts` 引用。
 */
export { applyInventoryAction } from '../../domains/inventory/services/actions';
