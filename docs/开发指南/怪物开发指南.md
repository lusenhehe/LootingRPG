# 怪物开发指南

> 适用版本：Phase 3.d 及以后  
> 配置文件路径：`assets/data/config/game/monsters.json`

---

## 一、快速上手

新增一只怪物，只需在 `monsters.json` 的 `"normal"` 或 `"boss"` 数组里添加一个对象。  
下面是一个**最小可运行**的普通怪模板：

```json
{
  "id": "cave_bat", //  
  "name": "Cave Bat",
  "icon": "🦇",
  "faction": "monster",
  "monsterType": "normal",
  "scalingProfile": "striker",
  "baseStats": { "hp": 0.88, "attack": 1.15, "defense": 0.75 },
  "elements": ["lightning"],
  "threatTypes": ["burst_punish"],
  "skills": ["basic_attack"],
  "passives": [],
  "aiProfile": "simple_aggressive",
  "lootTable": "generic_t1",
  "difficultyWeight": 1,
  "background": "monster.background.cave_bat"
}
```

> 💡 **字段不需要全部填写**，只保留必填项（见下节）也能正常运行。

---

## 二、字段详解

### 2.1 基础标识（必填）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | **全局唯一**，英文小写下划线，如 `cave_bat` |
| `name` | string | 显示名称（英文），后续可对接 i18n |
| `icon` | string | 一个 emoji，作为战斗界面头像 |
| `monsterType` | string | `"normal"` / `"elite"` / `"boss"` |
| `faction` | string | 固定填 `"monster"` |

---

### 2.2 数值三围 `baseStats`（必填）

```json
"baseStats": { "hp": 1.0, "attack": 1.0, "defense": 1.0 }
```

> ⚠️ **这里填的是倍率，不是绝对值！** 1.0 = 地图基准，0.8 = 弱化 20%，1.3 = 增强 30%。
**实际数值的计算流程**（你不需要手动计算，引擎自动完成）：
```
地图基准（1级）: HP=120  攻=16  防=7
每级增加:        HP+14   攻+1.35 防+0.72

模板值 = 地图基准 × baseStats × scalingProfile 系数
最终值 = 模板值 × 等级系数 × 遭遇系数 × 类型系数
```

**数值参考范围**（普通怪）：

| 定位 | hp | attack | defense |
|---|---|---|---|
| 肉盾 | 1.3 ~ 1.6 | 0.7 ~ 0.9 | 1.1 ~ 1.4 |
| 均衡 | 0.9 ~ 1.1 | 0.95 ~ 1.1 | 0.9 ~ 1.1 |
| 输出 | 0.7 ~ 0.95 | 1.1 ~ 1.5 | 0.6 ~ 0.85 |
| Boss | 3.0 ~ 4.5 | 2.0 ~ 3.0 | 2.5 ~ 3.5 |

---

### 2.3 缩放模板 `scalingProfile`（必填）

从以下预设中选一个，它会在 `baseStats` 基础上再乘一次系数：

| 值 | HP系数 | 攻击系数 | 防御系数 | 适合定位 |
|---|---|---|---|---|
| `normal` | ×1.0 | ×1.0 | ×1.0 | 普通均衡怪 |
| `tank` | ×1.4 | ×0.8 | ×1.2 | 肉盾、前排墙 |
| `glass` | ×0.7 | ×1.5 | ×0.6 | 脆皮大炮 |
| `bruiser` | ×1.2 | ×1.2 | ×0.9 | 攻守兼备的强壮怪 |
| `striker` | ×0.9 | ×1.3 | ×0.8 | 快打轻甲 |
| `boss` | ×1.8 | ×1.3 | ×1.25 | Boss 专用 |

**例子**：  
`baseStats.attack = 1.1` + `scalingProfile = glass` → 实际攻击系数 = 1.1 × 1.5 = 1.65（比基准强65%）

---

### 2.4 威胁类型 `threatTypes`（建议填）

描述这只怪对玩家的施压方式，影响 AI 提示文本，可多选：

| 值 | 含义 |
|---|---|
| `burst_punish` | 爆发型，集中输出，靠一波打垮玩家 |
| `sustain_pressure` | 持续型，磨耗玩家 |
| `tank_breaker` | 破防型，克制高防玩家 |
| `attrition` | 消耗型，靠 DOT/持续效果慢慢磨 |

---

### 2.6 元素属性 `elements`（可选）

可以填多个元素，影响元素反应触发：

| 值 | 备注 |
|---|---|
| `fire` | 火 |
| `poison` | 毒 |
| `ice` | 冰 |
| `water` | 水 |
| `lightning` | 雷 |

元素组合反应见[元素开发指南](技能与元素开发指南.md)。

---

### 2.7 技能 `skills` & 被动 `passives`（可选）

填写在 `skills.json` 中定义的技能ID：

```json
"skills": ["poison_blade", "basic_attack"],
"passives": []
```

- `basic_attack`：所有怪都可用的基础攻击，无需额外定义
- 自定义技能需先在 `skills.json` 添加（见技能开发指南）

---

### 2.8 标签 `tags`（可选）

自由标签，用于分类筛选，不影响战斗数值，示例：
`"beast"`, `"undead"`, `"slime"`, `"humanoid"`, `"demon"`, `"plant"`

---

### 2.9 AI行为 `aiProfile`（可选）

| 值 | 说明 |
|---|---|
| `simple_aggressive` | 简单进攻型，优先攻击玩家（默认） |

> 后续会扩展更多 AI 模式。

---

### 2.10 Boss 专属字段

Boss 需要额外填写：

```json
"bossIdentity": {
  "theme": "abyss",
  "introLineKey": "boss.abyss_demon.introLine",
  "battleLogLineKey": "boss.abyss_demon.battleLogLine",
  "phasePrompts": {
    "entering": "boss.abyss_demon.phase.entering",
    "fighting": "boss.abyss_demon.phase.fighting",
    "dying": "boss.abyss_demon.phase.dying",
    "dropping": "boss.abyss_demon.phase.dropping"
  }
},
"counterGoal": {
  "titleKey": "counter.abyss_demon",
  "stat": "defense",
  "threshold": 110,
  "successText": "counterSuccess.abyss_demon",
  "failText": "counterFail.abyss_demon"
}
```

**`counterGoal.stat`** 可选值：
`attack` / `defense` / `hp` / `elemental` / `lifesteal` / `thorns` / `attackSpeed`

> 玩家达到阈值时，Boss 攻击-5%、防御-10%；未达到时 HP+22%、攻击+18%、防御+15%。

---

## 三、完整 Boss 示例

```json
{
  "id": "frost_golem",
  "name": "Frost Golem",
  "icon": "🗿",
  "faction": "monster",
  "monsterType": "boss",
  "scalingProfile": "boss",
  "baseStats": { "hp": 4.2, "attack": 2.6, "defense": 3.1 },
  "elements": ["ice", "water"],
  "threatTypes": ["tank_breaker", "sustain_pressure"],
  "skills": ["basic_attack", "flame_shield"],
  "passives": [],
  "aiProfile": "simple_aggressive",
  "difficultyWeight": 5,
  "background": "monster.background.frost_golem",
  "bossIdentity": {
    "theme": "frost",
    "introLineKey": "boss.frost_golem.introLine",
    "battleLogLineKey": "boss.frost_golem.battleLogLine",
    "phasePrompts": {
      "entering": "boss.frost_golem.phase.entering",
      "fighting": "boss.frost_golem.phase.fighting",
      "dying": "boss.frost_golem.phase.dying",
      "dropping": "boss.frost_golem.phase.dropping"
    }
  },
  "counterGoal": {
    "titleKey": "counter.frost_golem",
    "stat": "attack",
    "threshold": 90,
    "successText": "counterSuccess.frost_golem",
    "failText": "counterFail.frost_golem"
  }
}
```

---

## 四、数值自检清单

提交前请自行检查：

- [ ] `id` 全局唯一，没有和现有怪物重名
- [ ] `baseStats` 是倍率（建议 HP 在 0.7 ~ 5.0 之间，超出请说明原因）
- [ ] 普通怪选了 `normal`/`tank`/`glass`/`bruiser`/`striker`，Boss 必须选 `boss`
- [ ] `monsterType = "boss"` 时填写了 `bossIdentity` 和 `counterGoal`
- [ ] `skills` 里的技能ID在 `skills.json` 中已存在

---

## 五、常见错误

| 错误 | 正确做法 |
|---|---|
| `"hp": 120` | 应填倍率 `"hp": 1.0`，不是绝对值 |
| `scalingProfile` 不填 | 默认会报错，务必填写 |
| Boss 没有 `bossIdentity` | Boss 进场时会显示异常 |
| `id` 与已有怪物重复 | 检查 `monsters.json` 全文 |

---

## 六、当前已有怪物一览

### 普通怪

| id | 名称 | 图标 | 定位 | 元素 | 特性 |
|---|---|---|---|---|---|
| `forest_slime` | Slime | 🟢 | 肉盾 | 毒 | lifesteal |
| `skeleton` | Skeleton | 💀 | 均衡 | — | — |
| `goblin` | Goblin | 👺 | 输出 | — | — |
| `wolf` | Wolf | 🐺 | 均衡 | — | — |
| `dark_spider` | Dark Spider | 🕷 | 脆皮爆发 | 毒 | lifesteal |
| `stone_golem` | Stone Golem | 🪨 | 超重甲 | — | thorns |
| `fire_imp` | Fire Imp | 🔥 | 脆皮大炮 | 火 | — |
| `ice_wolf` | Ice Wolf | 🐺 | 快打 | 冰/水 | double_attack |
| `thunder_eagle` | Thunder Eagle | 🦅 | 快打 | 雷 | double_attack |
| `plague_mushroom` | Plague Mushroom | 🍄 | 持续消耗 | 毒/水 | thorns |

### Boss

| id | 名称 | 图标 | 定位 | 元素 | 特性 |
|---|---|---|---|---|---|
| `abyss_demon` | Abyss Demon | 👹 | 狂暴型 | — | rage_on_low_hp |
| `venom_hydra` | Venom Hydra | 🐉 | 吸血消耗型 | 毒/水 | lifesteal + rage_on_low_hp |

---

## 七、地图节点波次（新怪物）

为了让新加入的怪物出现在野外，我们在 `ChapterData.json` 中为部分节点设定了具体波次。本节列出它们所在位置：

- **1-1 侦查营地**：所有波次都混有 `dark_spider` 和 `plague_mushroom`，帮助玩家熟悉毒系怪。
- **1-2 苔石峡谷**：三波精英怪，`stone_golem` 搭配 `dark_spider` 或 `plague_mushroom`，体现重甲 + 毒伤对抗。
- **1-3 林地小径**：三波普通怪，包含 `forest_slime`、`dark_spider`、`plague_mushroom` 的组合。
- **1-4 荒树基地**：两波精英怪，`stone_golem` 为主，分别配备 `dark_spider` 和 `plague_mushroom`。
- **1-5 古井之石**：单波精英，`ice_wolf` 与 `dark_spider` 的冰毒混战。
- **1-6 倒塌断桥**：单波普通，`thunder_eagle` + `forest_slime`，感受雷电突袭。
- **1-7 密林祭坛**：单波精英，`stone_golem`、`thunder_eagle`、`dark_spider` 三足鼎立。
- **1-8 蛮兽洞穴**：单波普通，`ice_wolf`、`forest_slime`、`plague_mushroom` 的多样敌群。
- **1-9 风暴草场**：单波精英，`thunder_eagle`、`plague_mushroom`、`dark_spider` 组成高机动组合。
- **1-10 林地首领巢穴**（boss节点）：先是两只 `dark_spider` 试探，然后出现主角 `abyss_demon`。
- **2-1 破碎回廊**：双波出现 `stone_golem`，作为入门级重甲精英。
- **3-1 熔火**：第一波加入 `fire_imp` 搭配 `lava_hound`。
- **3-2 灰烬祭坛**：精英波出现 `ice_wolf`、`lava_hound` 的混合。
- **3-3 炽热小径**：普通波由 `thunder_eagle` 与 `lava_hound` 组成。
- **4-10 冥灯守望**（boss节点）：唯一波为新 Boss `venom_hydra`。

请根据设计继续扩充其他节点。