[ok] 1.全项目
    [ok]全项目开启 strict（现在只是 skipLibCheck），尽量让 TypeScript 对所有分支做完全检查。
[ok]⚙️ 2. React/渲染性能
    [ok]给纯展示组件加上 React.memo。
    [ok]对在地图、背包等大量节点上使用的回调，包裹 useCallback，避免无谓重渲染。
    [考虑中]数据量大的列表（比如怪物图鉴）考虑用 react-window/react-virtualized 做虚拟列    表。
    [ok]计算属性（战斗数值、装备总和等）抽成 useMemo 或纯 util 函数，避免每次 render 重新计算。
[ok]📦 3. 构建 & 资源优化
    [--]配置生产环境的 Vite 插件压缩图片、SVG、移除 console。
    [ok]采用按需加载 / code‑splitting：场景切换时才加载 Forge、地图编辑器等。
    [ok]减少打包体积：审查依赖（node_modules），移除未使用的库。
[]🧹 4. 代码质量 & 可维护性
    []引入 ESLint/Prettier，统一风格，开启 @typescript-eslint/no-unused-vars 等严格规则。
    [ok]对重复逻辑（装备词缀遍历、伤害计算、地图路径搜索）抽出共享 util。
    []把大型配置文件（asset/data/config/map/mapdata.json、asset/data/config/game/equipment.json）拆分并加上类型定义。
    []增加 JSDoc/TypeScript 注释，方便后来维护。
[--no-need]🧪 5. 测试与 CI
    []用 Jest/React Testing Library 写一些单元/快照测试，特别是核心逻辑（battleEngine.ts、inventory.ts）。
    []在 GitHub Actions/其他 CI 中添加 npm run lint && npm run type-check && npm test，避免回归。
    
[]问题: useBattleActions.ts 中大量使用 any 类型 (battleState: any, setBattleState: React.[]Dispatch<React.SetStateAction<any>>)
[]- 建议: 导入并使用 BattleState 类型，替换所有 any
2. 重复代码 (DRY 原则)
[ok]- battleEngine.ts:63-78 和 battleEngine.ts:171-186 几乎相同的结果返回逻辑
[ok]- useBattleActions.ts 中战斗结束后的状态重置代码重复多次，建议提取为 resetBattleState() 函数
3. 魔法数字
[ok]- battleEngine.ts 中多处硬编码数值：frameStep = 260, frameStartDelay = 760, bonusTurns 计算等- ✅ 已抽离到 `src/config/game/battleConfig.ts` 并在相关 hook 中统一引用。

4. 组件优化
- InventoryTab.tsx:76-78 使用 useCallback 包裹单参数函数，可简化为直接传递
- 大型组件如 InventoryTab 超过 180 行，建议拆分为子组件（排序控制、物品列表等）
5. 状态管理
- [ok]gameState.ts 存在冗余导出，同时存在 src/logic/gameState.ts 和 src/logic/gameState/index.ts
- [ok]建议统一使用模块化导出，避免重导出链过长

7. 代码一致性
[x]- 混合使用中文 (暴击率, 背包) 和英文 (critRate, attack) 命名（已将内部所有相关字段迁移为英文，增设 nameConversion.ts 处理旧存档）
[x]- 建议统一命名规范，或建立明确的命名转换层；当前已通过辅助函数完成平滑过渡
8. 性能优化
[ok]- equipment.ts:53 使用 JSON.parse(JSON.stringify(item)) 深拷贝，可改为 structuredClone()
[ok]- 装备生成函数 generateEquipment 和 createCustomEquipment 有大量重复逻辑，可合并
10. Lint 配置
- package.json 中缺少 ESLint 配置
- 建议添加 ESLint + Prettier 统一代码风格